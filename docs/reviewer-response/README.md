# Reviewer-response evidence

The PDF and LaTeX source in this directory summarize a preserved result snapshot and the empirical
questions it can address. They are drafting aids, not CI outputs. Numerical claims should be updated
only from an artifact set that passes `verify_publication_artifacts.jl` for the corresponding source
commit and configuration.
